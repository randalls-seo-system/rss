"""Side-by-side diff of v2 output against v1 baseline article.

Outputs markdown diff showing word counts, link counts, callout counts,
and validator pass rates side-by-side. Manual tool for review during
iteration — not part of the automated pipeline.

Usage: python3 compare-to-baseline.py --v1-html <path> --v2-html <path> --output <path>

See docs/v2-module-architecture.md "tools/compare-to-baseline.py" for spec.
"""

raise NotImplementedError("compare-to-baseline.py is a stub — implement per architecture spec")
